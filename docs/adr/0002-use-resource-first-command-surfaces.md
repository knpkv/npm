# Use resource-first command surfaces

The CLI packages will use product-specific binaries with resource-first Canonical Commands for domain objects, shared Command Conventions for cross-product behavior, and Sync Workflow Commands for local-to-remote reconciliation. Breaking changes are acceptable because the goal is to make the documented and agent-facing command surfaces simpler and more uniform rather than preserve legacy verb-first spellings.
