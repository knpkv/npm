---
"@knpkv/control-center": minor
---

Add the versioned typed HTTP API, authenticated Node runtime, immutable static application, first-run browser pairing, plugin configuration persistence, and initial dark Control Center shell.
