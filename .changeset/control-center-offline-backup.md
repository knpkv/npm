---
"@knpkv/control-center": minor
---

Add offline `backup`, `verify-backup`, and `restore` commands with verified archives, non-mutating private SQLite snapshots, atomic no-clobber restore, stable terminal output, cause-preserving failures, and static boundary checks for the public backup API.
