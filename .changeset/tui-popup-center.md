---
"@knpkv/jira-clockify": patch
---

Center the title and message lines in the TUI popup dialog (`PopupMessage`) so all content — title, lines, and action buttons — is horizontally centered, instead of the title/lines being left-aligned while the buttons were centered.
