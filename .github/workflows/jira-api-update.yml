name: Jira API Spec Check

on:
  # Daily at 6 AM UTC (offset by 30 min from Confluence check)
  schedule:
    - cron: "30 6 * * *"

  # Manual trigger
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  check-spec:
    name: Check Jira API Spec Updates
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install dependencies
        uses: ./.github/actions/setup
        with:
          node-version: "24.10.0"

      - name: Check for spec updates
        id: check
        run: |
          if pnpm --filter @knpkv/jira-api-client regenerate:check; then
            echo "updated=false" >> $GITHUB_OUTPUT
          else
            echo "updated=true" >> $GITHUB_OUTPUT
          fi
        continue-on-error: true

      - name: Regenerate specs
        if: steps.check.outputs.updated == 'true'
        run: pnpm --filter @knpkv/jira-api-client regenerate

      - name: Build package
        if: steps.check.outputs.updated == 'true'
        run: pnpm --filter @knpkv/jira-api-client build

      - name: Run tests
        if: steps.check.outputs.updated == 'true'
        run: pnpm --filter @knpkv/jira-api-client test

      - name: Create changeset
        if: steps.check.outputs.updated == 'true'
        run: |
          mkdir -p .changeset
          cat > .changeset/jira-api-spec-update.md <<'CHANGESET'
          ---
          "@knpkv/jira-api-client": patch
          ---

          Update generated Jira API OpenAPI specs.
          CHANGESET

      - name: Create Pull Request
        if: steps.check.outputs.updated == 'true'
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "chore(jira-api-client): update OpenAPI specs"
          title: "chore(jira-api-client): update OpenAPI specs"
          body: |
            Automated update of Jira API OpenAPI specs.

            Review the generated schema changes and update wrapper files if needed:
            - `packages/jira-api-client/src/JiraApiClient.ts`
            - `packages/jira-api-client/src/generated/v3/index.ts`

            This PR includes a patch changeset so the release workflow can create the version plan.
          branch: chore/jira-api-spec-update
          delete-branch: true
          labels: |
            dependencies
            automated
