# yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/typescript_rule.json

id: use-tagged-error
language: tsx
severity: error
message: "Use Data.TaggedError instead of extending native Error"

rule:
  any:
    - pattern: class $NAME extends Error { $$$BODY }
    - all:
        - pattern: class $NAME { $$$BODY }
        - has:
            all:
              - kind: public_field_definition
              - regex: ^readonly\s+_tag\s*=
            stopBy: end

constraints:
  NAME:
    regex: "(Error|Failure|Unavailable)$"

files:
  - "packages/**/*.ts"
  - "packages/**/*.tsx"
  - "scripts/**/*.ts"

ignores:
  - "**/*.test.ts"
  - "**/*.spec.ts"
  - "**/generated/**"
  - "repos/**"
  - "scratchpad/**"

note: |
  Tagged errors give Effect code a stable _tag for catchTag/catchTags and searchable domain failures.
  Do not hand-roll a tagged failure by declaring `_tag` directly on a class.

  Prefer:

  export class SomeError extends Data.TaggedError("SomeError")<{
    readonly cause: unknown
  }> {}
