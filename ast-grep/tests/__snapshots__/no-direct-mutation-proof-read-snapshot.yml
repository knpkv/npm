id: no-direct-mutation-proof-read
snapshots:
  ? |
    const csrfToken = sessionStorage.getItem("cc_csrf")
  : labels:
      - source: sessionStorage.getItem("cc_csrf")
        style: primary
        start: 18
        end: 51
