/**
 * Shared browser/server API contracts.
 *
 * The authenticated HTTP contract is introduced by the tracer slice. This
 * entry exists now so its browser-safe boundary is stable from the first
 * package release.
 *
 * @packageDocumentation
 */
export {}
