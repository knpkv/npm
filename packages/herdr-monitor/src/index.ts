/** Explicit published status contracts. Server and publisher are separate opt-in entry points. */
export {
  AgentStatus,
  BoardId,
  BoardView,
  decodeSnapshot,
  MAX_BYTES,
  RETENTION_MS,
  Snapshot,
  STALE_MS
} from "./model.js"
