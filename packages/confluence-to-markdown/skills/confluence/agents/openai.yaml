interface:
  display_name: "Confluence Markdown CLI"
  short_description: "Sync and export Confluence pages as markdown"
  default_prompt: "Use $confluence to clone, fetch, pull, edit, or push Confluence pages as markdown while preserving ADF fidelity markers."
