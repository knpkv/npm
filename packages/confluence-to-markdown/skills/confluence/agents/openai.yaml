interface:
  display_name: "Confluence Markdown CLI"
  short_description: "Sync and export Confluence pages as markdown"
  default_prompt: "Use $confluence to run workspace, sync, and page commands for Confluence markdown while preserving ADF fidelity markers."
