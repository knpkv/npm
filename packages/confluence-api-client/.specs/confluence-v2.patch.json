[
  {
    "op": "replace",
    "path": "/components/schemas/PageSingle/properties/position",
    "value": {
      "description": "Position of the page within its parent page tree.",
      "anyOf": [{ "type": "integer", "format": "int32" }, { "type": "null" }]
    }
  },
  {
    "op": "replace",
    "path": "/components/schemas/PageBulk/properties/position",
    "value": {
      "description": "Position of the page within its parent page tree.",
      "anyOf": [{ "type": "integer", "format": "int32" }, { "type": "null" }]
    }
  },
  {
    "op": "replace",
    "path": "/components/schemas/ChildPage/properties/childPosition",
    "value": {
      "description": "Position of the child page within its parent page tree.",
      "anyOf": [{ "type": "integer", "format": "int32" }, { "type": "null" }]
    }
  },
  {
    "op": "replace",
    "path": "/components/schemas/ChildrenResponse/properties/childPosition",
    "value": {
      "description": "Position of the child within its parent content tree.",
      "anyOf": [{ "type": "integer", "format": "int32" }, { "type": "null" }]
    }
  },
  {
    "op": "replace",
    "path": "/components/schemas/FolderSingle/properties/position",
    "value": {
      "description": "Position of the folder within its parent page tree.",
      "anyOf": [{ "type": "integer", "format": "int32" }, { "type": "null" }]
    }
  },
  {
    "op": "replace",
    "path": "/components/schemas/FolderSingle/properties/createdAt",
    "value": {
      "description": "When the folder was created. The upstream spec declares an ISO-8601 string, but the v2 folder endpoints return epoch milliseconds (verified live against GET /folders/{id}), so both are accepted.",
      "anyOf": [{ "type": "string", "format": "date-time" }, { "type": "integer", "format": "int64" }]
    }
  },
  {
    "op": "replace",
    "path": "/components/schemas/FolderSingle/properties/parentId",
    "value": {
      "description": "ID of the parent content, or null if there is no parent content.",
      "anyOf": [{ "type": "string" }, { "type": "null" }]
    }
  },
  {
    "op": "replace",
    "path": "/components/schemas/FolderSingle/properties/parentType",
    "value": {
      "description": "Content type of the parent, or null if there is no parent.",
      "anyOf": [{ "$ref": "#/components/schemas/ParentContentType" }, { "type": "null" }]
    }
  },
  {
    "op": "replace",
    "path": "/components/schemas/AttachmentSingle/properties/mediaTypeDescription",
    "value": {
      "description": "Media Type description for the attachment. Atlassian may return null.",
      "anyOf": [{ "type": "string" }, { "type": "null" }]
    }
  },
  {
    "op": "replace",
    "path": "/components/schemas/AttachmentBulk/properties/mediaTypeDescription",
    "value": {
      "description": "Media Type description for the attachment. Atlassian may return null.",
      "anyOf": [{ "type": "string" }, { "type": "null" }]
    }
  }
]
